To configure username: git config --global user.name "New Name"
To configure email: git config --global user.email "New Email"
To check git account settings: git config --list
To check one item from the settings list: git config item.name

pwd: print working directory
cd: change directory
~: home
git rm file.txt: deletes file.txt (from repository after a commit)

git init: Initializes a Git repository
git add . : add all the changes made to current (.) directory 
git add file.txt: add file.txt in the "staging area", it goes from the working copy to the staging area. When a commit command is typed, then it goes into the repository
git commit -m "Message": creates sort of screenshot of current state of files
git log: gives history of commits
git commit -am ==> git add . + git commit -m, commits directly to the repository

git log --author="FullName or PartOfName of author": shows commits of a specific author
git diff: shows modifications that were made (working copy vs repository copy). If everything is up to date, it shows nothing.
git diff --staged: shows modifications between working copy vs staging copy

git status: compares the working directory to the repository (mumbo jumbo)
git checkout: takes repository copy replace working copy ==> cancels changes made to working copy not yet committed



--	To rename a file	--
	1rst Way
	------------
	rename the file
	git add newFileName
	git rm oldFileName

	2nd Way
	----------
	git mv oldFileName	 newFileName

	To initialize a repository
	-------------------------------
	cd DirectoryName
	git init	//Initializes an empty git repository DirectoryName
	git add .. 	//Adds all files and subdirectories in DirectoryName
	git commit -m "Adding DirectoryName" 	

	To unstage a file
	----------------------
	git reset HEAD fileName.extension

	To revert to a previous commit
	-----------------------------------------
	git checkout partOfCheckoutId -- fileName.extension
	git commit -m "Committed back to previous commit"

	Setting up a remote to your github page
	-----------------------------------------------------
	git remote add githubAlias https://github.com/username/repository.git	//Creating an alias for the webpage
	git remote	//Prints github remote alias

	To push files to public repository on server/github
	git push -u githubAlias master	//Asks you for your username & password
